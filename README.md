# Scrolls - releases

Public release channel for Scrolls. Installers (`Scrolls-Setup-*.exe`) and the
electron-updater feed (`latest.yml`) are published here by CI from the private
source repo. See the app for what Scrolls is.
